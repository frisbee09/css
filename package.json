{
  "name": "@cj/css",
  "version": "1.0.0",
  "dependencies": {
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@babel/core": "^7.14.0",
    "@babel/preset-env": "^7.14.1",
    "@babel/preset-typescript": "^7.13.0",
    "@types/jest": "^26.0.23",
    "babel-jest": "^26.6.3",
    "concurrently": "^6.1.0",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1"
  },
  "scripts": {
    "test": "jest",
    "format": "prettier --write \"src/**/*.ts\"",
    "build": "tsc",
    "checkTypes": "tsc --noEmit",
    "prepare": "yarn build",
    "preversion": "concurrently \"yarn checkTypes\" \"yarn test\""
  }
}
